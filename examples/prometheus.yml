global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: "oneview"
    scrape_interval: 60s
    scrape_timeout: 10s
    static_configs:
      - targets: ["oneview-exporter:9130"]
